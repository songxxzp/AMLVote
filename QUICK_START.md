# 学术作品投票平台

## 🚀 一键启动

### Windows 用户
```cmd
# 双击运行
start.bat

# 或命令行运行
.\start.bat
```

### macOS/Linux 用户
```bash
# 运行启动脚本
./start.sh

# 或手动启动
bun install
bun run db:generate
bun run db:push
bunx ts-node prisma/seed.ts
bun run dev
```

### Docker 用户
```bash
# 使用 Docker Compose
docker-compose up -d

# 或手动构建
docker build -t voting-platform .
docker run -p 3000:3000 voting-platform
```

## 📱 访问地址

启动成功后，请在浏览器中访问：
**http://localhost:3000**

## 🔧 系统功能

- ✅ **作品提交**: 支持论文、海报、演示项目
- ✅ **学号认证**: 基于学号的唯一身份识别  
- ✅ **投票限制**: 每位同学最多5票
- ✅ **实时排行**: TOP 10作品排行榜
- ✅ **文件上传**: 支持PDF、Word、PPT等格式
- ✅ **搜索过滤**: 按类型、关键词、作者搜索

## 📞 故障排除

### 端口问题
如果3000端口被占用，请修改：
- `start.sh` 或 `start.bat` 中的端口号
- 或使用 `PORT=3001 bun run dev`

### 数据库问题
如果数据库出错，请重置：
```bash
bun run db:push --force-reset
bunx ts-node prisma/seed.ts
```

### 依赖问题
如果依赖安装失败，请清除缓存：
```bash
rm -rf node_modules bun.lockb
bun install
```

---

**🎉 祝您使用愉快！**